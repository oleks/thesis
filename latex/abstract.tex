\begin{abstract}

A programming language $P$ may ``capture'' a complexity class $C$, in the sense
that all functions computable using $P$ lie in $C$, and all functions that lie
in $C$ are computable using $P$.

\end{abstract}
